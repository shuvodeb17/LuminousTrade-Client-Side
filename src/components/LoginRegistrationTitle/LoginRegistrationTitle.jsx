
const LoginRegistrationTitle = ({ title }) => {
    return (
        <h1 className='text-5xl font-bold text-center text-[#fff]' title={title}>{title}</h1>
    );
};

export default LoginRegistrationTitle;